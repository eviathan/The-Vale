namespace StardewTools.Ipc;

public static class PipeNames
{
    /// <summary>Local-only named pipe (Unix domain socket under the hood on macOS/Linux) the Trainer mod listens on.</summary>
    public const string Trainer = "StardewTools.Trainer";
}
